<x-layouts.app title="Struktur organisasi" description="Struktur pengelola MA Ma’arif NU Assa’adah."><x-page-header eyebrow="Profil" title="Struktur organisasi" description="Susunan pengelola madrasah dan bidang tanggung jawabnya." /><section class="py-14 sm:py-20"><div class="container-app">@if($members->isNotEmpty())<ul role="list" class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">@foreach($members as $member)<li class="rounded-2xl bg-white p-6 text-center ring-1 ring-slate-900/10">@if($member->photo)<img src="{{ asset('storage/'.$member->photo) }}" alt="" class="mx-auto aspect-square size-24 rounded-full object-cover outline outline-1 -outline-offset-1 outline-black/5">@endif<h2 class="mt-4 text-xl font-semibold text-slate-950">{{ $member->name }}</h2><p class="mt-1 text-base text-slate-500">{{ $member->position }}</p>@if($member->children->isNotEmpty())<ul role="list" class="mt-5 divide-y divide-slate-900/10 border-t border-slate-900/10 text-left">@foreach($member->children as $child)<li class="py-3"><p class="font-medium text-slate-900">{{ $child->name }}</p><p class="text-sm text-slate-500">{{ $child->position }}</p></li>@endforeach</ul>@endif</li>@endforeach</ul>@else<x-empty-state icon="network" title="Struktur belum tersedia" description="Susunan organisasi akan tampil setelah dikelola oleh administrator." />@endif</div></section></x-layouts.app>
