<x-layouts.app title="Galeri video" description="Dokumentasi video kegiatan madrasah."><x-page-header eyebrow="Galeri" title="Galeri video" description="Dokumentasi audiovisual kegiatan dan karya warga madrasah." /><section class="py-14 sm:py-20"><div class="container-app">@if($videos->isNotEmpty())<div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">@foreach($videos as $video)<article class="overflow-hidden rounded-2xl bg-white ring-1 ring-slate-900/10"><div class="aspect-video bg-primary-950">@if($video->embed_url)<iframe src="{{ $video->embed_url }}" title="{{ $video->title }}" class="size-full" loading="lazy" allowfullscreen></iframe>@elseif($video->thumbnail)<img src="{{ $video->thumbnail }}" alt="" loading="lazy" class="size-full object-cover">@endif</div><div class="p-6"><p class="font-medium text-primary-700">{{ optional($video->video_date)->translatedFormat('d F Y') }}</p><h2 class="mt-2 text-xl font-semibold tracking-tight text-slate-950">{{ $video->title }}</h2>@if($video->description)<p class="mt-2 line-clamp-3 text-base text-slate-600">{{ $video->description }}</p>@endif</div></article>@endforeach</div><div class="mt-10">{{ $videos->links() }}</div>@else<x-empty-state icon="video" title="Galeri video belum tersedia" />@endif</div></section></x-layouts.app>
