<x-layouts.app title="Program unggulan" description="Program unggulan MA Ma’arif NU Assa’adah."><x-page-header eyebrow="Akademik" title="Program unggulan" description="Pembinaan terarah untuk mengembangkan potensi, karakter, dan keterampilan peserta didik." /><section class="bg-white py-14 sm:py-20"><div class="container-app">@if($programs->isNotEmpty())<div class="grid gap-8 lg:grid-cols-2">@foreach($programs as $program)<article class="grid overflow-hidden rounded-2xl bg-primary-950 text-white sm:grid-cols-[2fr_3fr]">@if($program->cover)<img src="{{ asset('storage/'.$program->cover) }}" alt="" loading="lazy" class="size-full min-h-56 object-cover">@else<div class="flex min-h-56 items-center justify-center bg-primary-900"><x-icon name="sparkles" class="size-10 text-gold-300" /></div>@endif<div class="p-7"><h2 class="text-2xl font-semibold tracking-tight">{{ $program->name }}</h2><p class="mt-3 text-pretty text-base text-primary-100">{{ $program->description }}</p>@if($program->highlights)<ul class="mt-5 list-disc space-y-2 pl-5 text-base text-primary-100">@foreach(preg_split('/\r\n|\r|\n/', $program->highlights) as $highlight)<li>{{ $highlight }}</li>@endforeach</ul>@endif</div></article>@endforeach</div>@else<x-empty-state icon="sparkles" title="Program unggulan belum tersedia" />@endif</div></section></x-layouts.app>
