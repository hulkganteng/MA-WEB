<x-layouts.app title="Kalender akademik" description="Kalender kegiatan akademik madrasah."><x-page-header eyebrow="Akademik" title="Kalender akademik" description="Jadwal kegiatan, ujian, libur, rapat, dan agenda akademik lainnya." /><section class="py-14 sm:py-20"><div class="container-app max-w-5xl"><div class="divide-y divide-slate-900/10 rounded-2xl bg-white px-6 ring-1 ring-slate-900/10">@forelse($events as $event)<article class="grid gap-4 py-6 sm:grid-cols-[10rem_1fr]"><div><p class="font-semibold tabular-nums text-primary-800">{{ $event->start_date->translatedFormat('d M Y') }}</p>@if($event->end_date && !$event->end_date->equalTo($event->start_date))<p class="text-sm text-slate-500">s.d. {{ $event->end_date->translatedFormat('d M Y') }}</p>@endif</div><div><span class="rounded-full bg-primary-50 px-3 py-1 text-sm font-medium text-primary-800">{{ ucfirst($event->category) }}</span><h2 class="mt-3 text-xl font-semibold tracking-tight text-slate-950">{{ $event->title }}</h2>@if($event->description)<p class="mt-2 text-pretty text-base text-slate-600">{{ $event->description }}</p>@endif</div></article>@empty<div class="py-8"><x-empty-state icon="calendar-days" title="Kalender akademik belum tersedia" /></div>@endforelse</div><div class="mt-10">{{ $events->links() }}</div></div></section></x-layouts.app>
