<x-layouts.app title="Pencarian" description="Cari informasi di website madrasah."><x-page-header title="Pencarian" description="Cari berita, pengumuman, agenda, prestasi, guru, halaman, dokumen, dan ekstrakurikuler." /><section class="py-14 sm:py-20"><div class="container-app max-w-5xl"><form action="{{ route('search') }}" method="GET" class="flex max-w-2xl flex-col gap-3 sm:flex-row"><div class="min-w-0 flex-1"><label for="search-query" class="sr-only">Kata kunci</label><input id="search-query" name="q" type="search" value="{{ $term }}" class="input" placeholder="Masukkan kata kunci"></div><button type="submit" class="btn-primary shrink-0"><x-icon name="search" class="size-4" /> Cari informasi</button></form>@if($term)<p class="mt-8 text-base text-slate-600">{{ $results->sum(fn($items) => $items->count()) }} hasil untuk “{{ $term }}”.</p>@endif<div class="mt-8 flex flex-col gap-8">@forelse($results as $group=>$items)<section><h2 class="text-xl font-semibold tracking-tight text-slate-950">{{ $group }}</h2><div class="mt-4 divide-y divide-slate-900/10 rounded-2xl bg-white px-6 ring-1 ring-slate-900/10">@foreach($items as $item)<article class="py-5"><h3 class="font-semibold text-slate-950"><a href="{{ $item['url'] }}" class="hover:text-primary-700">{{ $item['title'] }}</a></h3>@if($item['description'])<p class="mt-2 line-clamp-2 text-base text-slate-600">{{ \Illuminate\Support\Str::limit($item['description'],180) }}</p>@endif</article>@endforeach</div></section>@empty@if($term)<x-empty-state icon="search-x" title="Tidak ada hasil" description="Coba gunakan kata kunci yang lebih umum atau periksa ejaannya." />@endif@endforelse</div></div></section></x-layouts.app>
