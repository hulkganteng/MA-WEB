<x-layouts.admin title="Halaman"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 class="text-2xl font-semibold tracking-tight text-slate-950">Kelola halaman</h2><p class="mt-2 text-base text-slate-600">Atur halaman profil dan informasi statis.</p></div>@can('pages.create')<a href="{{ route('admin.pages.create') }}" class="btn-primary shrink-0 !py-2 !pr-3 !pl-2"><x-icon name="plus" class="size-4" />Tambah halaman</a>@endcan</div><form method="GET" class="mt-7 flex gap-3"><div class="min-w-0 flex-1"><label for="q" class="sr-only">Cari halaman</label><input id="q" name="q" type="search" value="{{ request('q') }}" class="input !py-2" placeholder="Cari halaman"></div><button type="submit" class="btn-outline !py-2">Cari</button></form><div class="mt-7 divide-y divide-slate-900/10">@forelse($pages as $page)<article class="flex flex-col justify-between gap-4 py-5 sm:flex-row sm:items-center"><div class="min-w-0"><h3 class="truncate font-semibold text-slate-950">{{ $page->title }}</h3><p class="mt-1 text-sm text-slate-500">/{{ $page->slug }} · {{ ucfirst($page->status) }} · {{ $page->updated_at->diffForHumans() }}</p></div><div class="flex shrink-0 gap-2"><a href="{{ route('pages.show',$page) }}" target="_blank" class="rounded-lg px-2.5 py-1.5 text-sm font-medium text-slate-600 hover:bg-slate-100">Lihat</a>@can('pages.update')<a href="{{ route('admin.pages.edit',$page) }}" class="rounded-lg px-2.5 py-1.5 text-sm font-medium text-primary-700 hover:bg-primary-50">Edit</a>@endcan @can('pages.delete')<form method="POST" action="{{ route('admin.pages.destroy',$page) }}" onsubmit="return confirm('Pindahkan halaman ini ke sampah?')">@csrf @method('DELETE')<button type="submit" class="rounded-lg px-2.5 py-1.5 text-sm font-medium text-rose-700 hover:bg-rose-50">Hapus</button></form>@endcan</div></article>@empty<p class="py-12 text-center text-base text-slate-500">Belum ada halaman.</p>@endforelse</div><div class="mt-7">{{ $pages->links() }}</div></x-layouts.admin>
